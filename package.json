{
  "name": "tiulii",
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "src/*"
  ],
  "scripts": {
    "build": "npm run build:shared && npm run build -w ./src/client && npm run build -w ./src/server",
    "build:shared": "npm run build -w ./src/shared",
    "bundle": "npm run build && node ./scripts/bundle.ts"
  },
  "devDependencies": {
    "rolldown": "^1.2.7",
    "typescript": "6.0.3"
  }
}

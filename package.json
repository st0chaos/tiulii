{
  "name": "tiulii",
  "workspaces": [
    "src/*"
  ],
  "scripts": {
    "build": "npm run build -w ./src/shared && npm run build -w ./src/client && node ./build-client.mjs && npm run build -w ./src/server && node ./build-server.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "6.0.3"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}

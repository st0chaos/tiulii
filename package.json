{
  "name": "tiulii",
  "type": "module",
  "workspaces": [
    "src/*"
  ],
  "scripts": {
    "build:shared": "npm run build -w ./src/shared",
    "build": "npm run build:shared && npm run build -w ./src/client && tsx ./scripts/build-client.ts && npm run build -w ./src/server && tsx ./scripts/build-server.ts"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "tsx": "^4.23.12",
    "typescript": "6.0.3"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}

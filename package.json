{
  "name": "tiulii",
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "src/*"
  ],
  "scripts": {
    "build": "npm run build -w src/shared && npm run build -w src/client && npm run build -w src/server",
    "bundle": "npm run build && node bundle.ts"
  },
  "devDependencies": {
    "rolldown": "^1.2.7",
    "typescript": "6.0.3"
  }
}
